<div class="container section--default">
    <div class="content-container {{ section }}">
        <ul class="breadcrumb">
            {% for item in items %}
                <li>{{ item }}</li>
            {% endfor %}
        </ul>
    </div>
</div>
